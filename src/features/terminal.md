# 集成终端

集成终端让你无需切换到外部终端工具，直接在 LoongCode 内运行命令行操作。它是真实的 PTY 终端（PTY 即伪终端，是操作系统提供的终端仿真接口，支持交互式程序如 `vim`、`top`、`npm install` 等正常运行）。AI 执行命令时也通过同一终端进行，你可以实时看到输出。

---

## 打开终端

**方法一**：点击顶部标题栏的**终端**按钮。

**方法二**：按快捷键 **Ctrl+`**（反引号，Tab 键上方）。

终端面板从中央对话区底部弹出，覆盖在对话区下方，不影响右侧面板的正常使用。再次点击**终端**按钮或按 Ctrl+` 可关闭面板。

![截图：集成终端面板从对话区底部弹出，显示 Shell 提示符，标题栏中「终端」按钮处于激活状态](...)

---

## 多标签页管理

终端面板支持同时开启多个终端会话：

- 点击面板顶部的**新建终端**按钮（+ 图标），新开一个独立的终端标签页。
- 点击标签页右侧的关闭按钮可**单独关闭**该终端，同时结束其子进程树，不影响其他标签页。
- 点击面板右上角的**关闭终端面板**按钮，关闭并结束当前工作区的**所有**终端。

::: tip
关闭终端会连同该终端启动的所有子进程一起结束，避免后台进程残留占用目录。
:::

---

## AI 与终端联动

AI（Agent）执行命令时会在当前工作区的终端中运行，你可以实时看到每条命令的完整输出——构建日志、测试结果、安装进度都逐行显示。

你同样可以在终端里直接输入命令，与 AI 协同工作：比如 AI 写好脚本后，你手动在终端里运行并把结果告诉 AI。

---

## 环境变量

终端继承工作区的环境变量配置。如需添加全局环境变量（对所有任务生效），请前往**设置 → 常规**进行配置；详见[设置 → 常规](/settings/general)。

---

## 下一步

了解终端之后，接下来看看如何管理 Git 分支、提交代码以及审查改动——前往 [Git 集成](/features/git)继续学习。
