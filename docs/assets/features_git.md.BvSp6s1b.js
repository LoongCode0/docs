import{_ as o,o as e,c as a,ag as s,ah as t}from"./chunks/framework.D9bIGSBm.js";const k=JSON.parse('{"title":"Git 集成","description":"","frontmatter":{},"headers":[],"relativePath":"features/git.md","filePath":"features/git.md"}'),i={name:"features/git.md"};function n(l,r,d,g,c,h){return e(),a("div",null,[...r[0]||(r[0]=[s('<h1 id="git-集成" tabindex="-1">Git 集成 <a class="header-anchor" href="#git-集成" aria-label="Permalink to &quot;Git 集成&quot;">​</a></h1><p>LoongCode 将常用的 Git 工作流直接集成到界面中：统一的分支与 worktree 切换器、可视化的代码审查面板、AI 一键生成提交消息，以及简洁的提交与推送入口。即使不熟悉 Git 命令行，也可以通过界面完成完整的版本管理流程。</p><blockquote><p>相关基础：<a href="/docs/guide/interface-overview.html">界面导览</a></p></blockquote><div class="tip custom-block"><p class="custom-block-title">不熟悉 Git？</p><p>没关系——本页的操作都可以照步骤点击完成，不需要记任何命令。Git 是记录代码每次修改的版本管理工具：「提交」相当于存一次档，「分支」相当于平行的存档线，「推送」则是把存档同步到云端仓库。先照着做，细节可以以后再深入。</p></div><hr><h2 id="分支与-worktree-切换器" tabindex="-1">分支与 Worktree 切换器 <a class="header-anchor" href="#分支与-worktree-切换器" aria-label="Permalink to &quot;分支与 Worktree 切换器&quot;">​</a></h2><p>顶部标题栏显示当前任务所在的分支名称，点击它即可打开统一切换器。</p><p>Worktree（Git 工作树）可以这样理解：同一份代码库在多个文件夹中以不同分支状态同时打开——每个文件夹各自停留在自己的分支上，互不干扰，适合多条任务并行开发。切换器将普通分支和 worktree 统一列出，一个入口管理全部。</p><p><img src="'+t+'" alt="截图：顶部标题栏分支切换器展开，显示「搜索分支 / worktree」输入框，列出本地分支与 worktree 分支"></p><h3 id="搜索与切换" tabindex="-1">搜索与切换 <a class="header-anchor" href="#搜索与切换" aria-label="Permalink to &quot;搜索与切换&quot;">​</a></h3><p>切换器顶部有<strong>搜索分支 / worktree</strong>输入框，直接输入关键词过滤列表。列表分为<strong>分支</strong>和<strong>远程跟踪</strong>两个分组（「远程跟踪」指与远程仓库同步的分支引用，形如 <code>origin/main</code>，代表远程仓库上对应分支的最新状态）：</p><ul><li>选择普通分支 → 对主仓库执行 <code>git checkout</code>（若当前在 worktree 跟随状态则先脱离）。</li><li>选择 worktree 占用的分支 → 切换到该 worktree 环境（不执行 checkout，改变的是 UI 面板根目录）。</li><li>底部列出<strong>未提交的更改</strong>文件数，切换前可确认当前工作区状态。</li></ul><h3 id="新建分支" tabindex="-1">新建分支 <a class="header-anchor" href="#新建分支" aria-label="Permalink to &quot;新建分支&quot;">​</a></h3><p>在切换器底部点击<strong>创建并检出新分支…</strong>，打开「创建并检出新分支」对话框：</p><ol><li>在<strong>分支名</strong>输入框填写新分支名称（例如 <code>feature/my-feature</code>）。</li><li>点击<strong>创建并切换</strong>，基于当前 HEAD 创建新分支并立即切换过去。</li></ol><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>目前仅支持基于当前 HEAD 创建新分支。</p></div><h3 id="新建-worktree" tabindex="-1">新建 Worktree <a class="header-anchor" href="#新建-worktree" aria-label="Permalink to &quot;新建 Worktree&quot;">​</a></h3><p>在切换器底部点击<strong>新建 worktree…</strong>，打开「新建 worktree」对话框：</p><ol><li>填写新分支名和<strong>worktree 目录</strong>（相对仓库的路径；默认会在仓库的上一级目录创建一个「仓库名-分支名」的文件夹，可手动修改）。</li><li>点击<strong>创建并跟随</strong>，创建新 worktree 并让当前任务自动跟随过去。</li></ol><h3 id="agent-自动跟随-worktree-v0-2-0-起" tabindex="-1">Agent 自动跟随 Worktree（v0.2.0 起） <a class="header-anchor" href="#agent-自动跟随-worktree-v0-2-0-起" aria-label="Permalink to &quot;Agent 自动跟随 Worktree（v0.2.0 起）&quot;">​</a></h3><p>当 AI（Agent）在对话中自动执行 <code>git worktree add</code> 创建 worktree 时，LoongCode 会侦测到这一操作并让当前任务自动跟随——<strong>文件树、终端、审查面板整体切换到新 worktree 目录</strong>，你无需手动操作。界面会弹出一条可撤销的提示，点击撤销仅停止跟随，不删除 worktree。</p><p>worktree 被删除时（无论是应用内操作还是命令行外部操作），绑定它的任务会自动脱离回主仓库，面板恢复到主树目录。</p><h3 id="移除-worktree" tabindex="-1">移除 Worktree <a class="header-anchor" href="#移除-worktree" aria-label="Permalink to &quot;移除 Worktree&quot;">​</a></h3><p>在切换器中将鼠标悬浮到 worktree 分支行，出现<strong>移除 worktree</strong> 入口（垃圾桶图标），点击后立即执行移除——移除前应用会先结束该 worktree 目录下运行的所有终端进程，再删除该 worktree 文件夹。</p><p>若该 worktree 中存在未提交的改动（或目录被占用），普通移除会失败：此时会弹出确认对话框，显示 Git 的原始错误信息，并询问「仍要强制移除吗？」。</p><div class="danger custom-block"><p class="custom-block-title">强制移除会丢失未提交的改动</p><p>普通移除在 worktree 有未提交改动时会被 Git 拒绝，改动不会丢失；但确认<strong>强制移除</strong>后，整个 worktree 文件夹连同其中未提交的改动会被一并删除，<strong>无法恢复</strong>。移除前请确认改动已提交，或已另行备份。</p></div><p>另外，在 worktree 环境下，顶部提交菜单中的<strong>创建分支</strong>功能不可用——界面会提示：请先移除当前 worktree（任务会退回主仓库分支），再创建分支。</p><hr><h2 id="审查面板" tabindex="-1">审查面板 <a class="header-anchor" href="#审查面板" aria-label="Permalink to &quot;审查面板&quot;">​</a></h2><p><strong>审查</strong>标签页在右侧面板中展示工作区的代码 Diff，帮你在提交前确认所有改动。</p><p><img src="'+t+'" alt="截图：右侧面板审查标签页，显示文件列表与红绿 Diff，顶部有「未暂存」「已暂存」「全部分支更改」「上一轮更改」四个模式标签，文件名右侧有目录段灰字"></p><h3 id="查看模式" tabindex="-1">查看模式 <a class="header-anchor" href="#查看模式" aria-label="Permalink to &quot;查看模式&quot;">​</a></h3><p>审查面板顶部有四个视图模式切换：</p><table tabindex="0"><thead><tr><th>模式</th><th>显示内容</th></tr></thead><tbody><tr><td><strong>未暂存</strong></td><td>尚未 <code>git add</code> 的工作区改动</td></tr><tr><td><strong>已暂存</strong></td><td>已 <code>git add</code> 但未提交的改动</td></tr><tr><td><strong>全部分支更改</strong></td><td>当前分支与基干分支（main/master）的所有差异</td></tr><tr><td><strong>上一轮更改</strong></td><td>上一次提交的改动（类似 <code>git show HEAD</code>）</td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>若仓库没有 main/master 或 origin/HEAD，「全部分支更改」模式会显示提示「未找到基干分支（main/master 或 origin/HEAD），无法比较全部分支更改」。</p></div><h3 id="文件列表与-diff" tabindex="-1">文件列表与 Diff <a class="header-anchor" href="#文件列表与-diff" aria-label="Permalink to &quot;文件列表与 Diff&quot;">​</a></h3><p>每个改动文件显示文件名与<strong>文件名右侧的目录段</strong>（灰色小字，过长时自动中段省略），方便在多个同名文件之间快速定位。每行还显示变更类型标记：<strong>增</strong>（新增文件）、<strong>删</strong>（删除文件）、<strong>改</strong>（修改文件）。</p><p>点击文件行即可展开内联 Diff，以行级红绿高亮呈现新旧内容对比。点击<strong>展开全部内容</strong>可查看未截断的完整 Diff。</p><hr><h2 id="提交代码" tabindex="-1">提交代码 <a class="header-anchor" href="#提交代码" aria-label="Permalink to &quot;提交代码&quot;">​</a></h2><h3 id="打开提交对话框" tabindex="-1">打开提交对话框 <a class="header-anchor" href="#打开提交对话框" aria-label="Permalink to &quot;打开提交对话框&quot;">​</a></h3><p>点击顶部标题栏的<strong>提交</strong>按钮（或在<strong>更多 Git 操作</strong>菜单中选择<strong>提交</strong>），打开「提交更改」对话框。</p><p>对话框显示：</p><ul><li><strong>当前分支</strong>：本次提交将写入哪条分支</li><li><strong>更改</strong>：将被提交的文件数</li><li><strong>提交消息</strong>输入框：填写本次提交的说明文字，占位提示为「留空以自动生成提交消息」</li></ul><h3 id="ai-生成提交消息-v0-5-0-起" tabindex="-1">AI 生成提交消息（v0.5.0 起） <a class="header-anchor" href="#ai-生成提交消息-v0-5-0-起" aria-label="Permalink to &quot;AI 生成提交消息（v0.5.0 起）&quot;">​</a></h3><p>不想自己写提交消息？有两种自动方式，效果和成本不同：</p><ul><li><strong>点 ✨ 按钮（AI 生成，可预览）</strong>：点击提交消息输入框右侧的 <strong>✨</strong> 按钮，AI 根据当前工作区的实际改动生成一条规范的提交消息并填入输入框；你可以在此基础上继续编辑，满意后再点击<strong>提交</strong>。AI 生成会<strong>消耗额外 token</strong>（走一次与当前任务隔离的一次性会话，不会出现在任务列表中）；生成失败时会显示具体的错误信息，便于排查。</li><li><strong>直接留空提交（本地摘要，不耗 token）</strong>：不填提交消息直接点击<strong>提交</strong>，会自动用改动文件名与增删行数拼出一条简单的摘要消息（形如 <code>update src/foo.ts (+12 -3)</code>）。这一条在本地生成，不调用 AI、不消耗 token。</li></ul><h3 id="推送与更多-git-操作" tabindex="-1">推送与更多 Git 操作 <a class="header-anchor" href="#推送与更多-git-操作" aria-label="Permalink to &quot;推送与更多 Git 操作&quot;">​</a></h3><p>点击<strong>提交</strong>按钮旁的<strong>更多 Git 操作</strong>按钮，展开操作菜单，包含：</p><ul><li><strong>提交</strong>：等同于点击提交按钮</li><li><strong>推送</strong>：将当前分支推送到 <code>origin</code>，推送期间界面显示「推送中…」提示，完成后显示「已推送到 origin/&lt;分支名&gt;」</li><li><strong>创建分支</strong>：基于当前 HEAD 新建并切换分支（worktree 环境下不可用）</li></ul><p><img src="'+t+'" alt="截图：顶部标题栏「更多 Git 操作」菜单展开，显示提交、推送、创建分支三个选项，突出推送项右侧的「↑2」未推送计数徽标"></p><h3 id="未推送计数" tabindex="-1">未推送计数 <a class="header-anchor" href="#未推送计数" aria-label="Permalink to &quot;未推送计数&quot;">​</a></h3><p>当本地有尚未推送的提交时，<strong>推送</strong>菜单项右侧会显示 <code>↑N</code> 徽标——N 表示本地领先远程的提交数，也就是已提交但还没推送到远程仓库的提交数量。推送成功后徽标消失。看到徽标就说明还有工作没同步到远程，记得及时推送。</p><hr><h2 id="回滚变更" tabindex="-1">回滚变更 <a class="header-anchor" href="#回滚变更" aria-label="Permalink to &quot;回滚变更&quot;">​</a></h2><p>代码回滚<strong>不是 Git 面板里的独立操作</strong>，而是「编辑历史消息」的一个附加选项：在编辑某条历史用户消息时勾选<strong>同时回滚代码</strong>，LoongCode 才会把工作区文件回滚到该消息发送前的状态，并显示<strong>本次回滚变更</strong>预览面板，展示将被回滚的文件列表与 Diff。</p><div class="danger custom-block"><p class="custom-block-title">回滚会连带改变对话历史</p><p>回滚依附于「编辑历史消息」，触发时会从该条消息处<strong>截断当前会话</strong>（其后的对话不再保留在当前任务中），再以修改后的内容重新运行——<strong>文件与对话历史会一起变化</strong>，不存在「只回滚文件、对话历史不变」的情况。该消息之后的文件改动（含你手动改的）也会被丢弃；回滚前 LoongCode 会自动存一份代码安全快照，但当前版本未提供一键撤销入口，请务必在确认 Diff 预览后再操作。</p></div><p>完整操作步骤与可用条件，见<a href="/docs/features/conversation.html#编辑历史消息重跑">对话面板 → 编辑历史消息重跑</a>与<a href="/docs/features/checkpoints.html">代码检查点</a>。</p><hr><h2 id="下一步" tabindex="-1">下一步 <a class="header-anchor" href="#下一步" aria-label="Permalink to &quot;下一步&quot;">​</a></h2><p>完成 Git 工作流的学习后，接下来了解如何使用斜杠命令快速调用内置功能——前往<a href="/docs/features/slash-commands.html">斜杠命令</a>继续学习。</p>',61)])])}const u=o(i,[["render",n]]);export{k as __pageData,u as default};
