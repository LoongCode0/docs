import{_ as o,o as e,c as a,ag as i,ah as t}from"./chunks/framework.D9bIGSBm.js";const k=JSON.parse('{"title":"Git 集成","description":"","frontmatter":{},"headers":[],"relativePath":"features/git.md","filePath":"features/git.md"}'),s={name:"features/git.md"};function n(l,r,d,g,c,h){return e(),a("div",null,[...r[0]||(r[0]=[i('<h1 id="git-集成" tabindex="-1">Git 集成 <a class="header-anchor" href="#git-集成" aria-label="Permalink to &quot;Git 集成&quot;">​</a></h1><p>LoongCode 将常用的 Git 工作流直接集成到界面中：统一的分支与 worktree 切换器、可视化的代码审查面板、AI 一键生成提交消息，以及简洁的提交与推送入口，让你不必离开应用就能完成完整的版本管理流程。</p><blockquote><p>相关基础：<a href="/docs/guide/interface-overview.html">界面导览</a></p></blockquote><hr><h2 id="分支与-worktree-切换器" tabindex="-1">分支与 Worktree 切换器 <a class="header-anchor" href="#分支与-worktree-切换器" aria-label="Permalink to &quot;分支与 Worktree 切换器&quot;">​</a></h2><p>顶部标题栏显示当前任务所在的分支名称，点击它即可打开统一切换器。</p><p>Worktree（Git 工作树）是指同一仓库在不同目录下同时检出多个分支的能力，常用于多任务并行开发。切换器将普通分支和 worktree 统一列出，一个入口管理全部。</p><p><img src="'+t+'" alt="截图：顶部标题栏分支切换器展开，显示「搜索分支 / worktree」输入框，列出本地分支与 worktree 分支"></p><h3 id="搜索与切换" tabindex="-1">搜索与切换 <a class="header-anchor" href="#搜索与切换" aria-label="Permalink to &quot;搜索与切换&quot;">​</a></h3><p>切换器顶部有<strong>搜索分支 / worktree</strong>输入框，直接输入关键词过滤列表。列表分为<strong>分支</strong>和<strong>远程跟踪</strong>两个分组：</p><ul><li>选择普通分支 → 对主仓库执行 <code>git checkout</code>（若当前在 worktree 跟随状态则先脱离）。</li><li>选择 worktree 占用的分支 → 切换到该 worktree 环境（不执行 checkout，改变的是 UI 面板根目录）。</li><li>底部列出<strong>未提交的更改</strong>文件数，切换前可确认当前工作区状态。</li></ul><h3 id="新建分支" tabindex="-1">新建分支 <a class="header-anchor" href="#新建分支" aria-label="Permalink to &quot;新建分支&quot;">​</a></h3><p>在切换器底部点击<strong>创建并检出新分支…</strong>，打开「创建并检出新分支」对话框：</p><ol><li>在<strong>分支名</strong>输入框填写新分支名称（例如 <code>feature/my-feature</code>）。</li><li>点击<strong>创建并切换</strong>，基于当前 HEAD 创建新分支并立即切换过去。</li></ol><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>目前仅支持基于当前 HEAD 创建新分支。</p></div><h3 id="新建-worktree" tabindex="-1">新建 Worktree <a class="header-anchor" href="#新建-worktree" aria-label="Permalink to &quot;新建 Worktree&quot;">​</a></h3><p>在切换器底部点击<strong>新建 worktree…</strong>，打开「新建 worktree」对话框：</p><ol><li>填写新分支名和<strong>worktree 目录</strong>（相对仓库的路径，默认按 <code>../&lt;仓库名&gt;-&lt;分支名&gt;</code> 自动推导）。</li><li>点击<strong>创建并跟随</strong>，创建新 worktree 并让当前任务自动跟随过去。</li></ol><h3 id="agent-自动跟随-worktree-v0-2-0-起" tabindex="-1">Agent 自动跟随 Worktree（v0.2.0 起） <a class="header-anchor" href="#agent-自动跟随-worktree-v0-2-0-起" aria-label="Permalink to &quot;Agent 自动跟随 Worktree（v0.2.0 起）&quot;">​</a></h3><p>当 AI（Agent）在对话中自动执行 <code>git worktree add</code> 创建 worktree 时，LoongCode 会侦测到这一操作并让当前任务自动跟随——<strong>文件树、终端、审查面板整体切换到新 worktree 目录</strong>，你无需手动操作。界面会弹出一条可撤销的提示，点击撤销仅停止跟随，不删除 worktree。</p><p>worktree 被删除时（无论是应用内操作还是命令行外部操作），绑定它的任务会自动脱离回主仓库，面板恢复到主树目录。</p><h3 id="移除-worktree" tabindex="-1">移除 Worktree <a class="header-anchor" href="#移除-worktree" aria-label="Permalink to &quot;移除 Worktree&quot;">​</a></h3><p>在切换器中将鼠标悬浮到 worktree 分支行，出现<strong>移除 worktree</strong> 入口，点击后二次确认即可移除。若因目录被占用或工作树有脏改动导致失败，会显示 git 原始错误信息，并提供<strong>仍要强制移除</strong>的选项。</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>在 worktree 环境下，顶部提交菜单中的<strong>创建分支</strong>功能不可用。请先移除当前 worktree（任务会退回主仓库分支），再创建分支。</p></div><hr><h2 id="审查面板" tabindex="-1">审查面板 <a class="header-anchor" href="#审查面板" aria-label="Permalink to &quot;审查面板&quot;">​</a></h2><p><strong>审查</strong>标签页在右侧面板中展示工作区的代码 Diff，帮你在提交前确认所有改动。</p><p><img src="'+t+'" alt="截图：右侧面板审查标签页，显示文件列表与红绿 Diff，顶部有「未暂存」「已暂存」「全部分支更改」「上一轮更改」四个模式标签，文件名右侧有目录段灰字"></p><h3 id="查看模式" tabindex="-1">查看模式 <a class="header-anchor" href="#查看模式" aria-label="Permalink to &quot;查看模式&quot;">​</a></h3><p>审查面板顶部有四个视图模式切换：</p><table tabindex="0"><thead><tr><th>模式</th><th>显示内容</th></tr></thead><tbody><tr><td><strong>未暂存</strong></td><td>尚未 <code>git add</code> 的工作区改动</td></tr><tr><td><strong>已暂存</strong></td><td>已 <code>git add</code> 但未提交的改动</td></tr><tr><td><strong>全部分支更改</strong></td><td>当前分支与基干分支（main/master）的所有差异</td></tr><tr><td><strong>上一轮更改</strong></td><td>上一次提交的改动（类似 <code>git show HEAD</code>）</td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>若仓库没有 main/master 或 origin/HEAD，「全部分支更改」模式会显示提示「未找到基干分支（main/master 或 origin/HEAD），无法比较全部分支更改」。</p></div><h3 id="文件列表与-diff" tabindex="-1">文件列表与 Diff <a class="header-anchor" href="#文件列表与-diff" aria-label="Permalink to &quot;文件列表与 Diff&quot;">​</a></h3><p>每个改动文件显示文件名与<strong>文件名右侧的目录段</strong>（灰色小字，过长时自动中段省略），方便在多个同名文件之间快速定位。每行还显示变更类型标记：<strong>增</strong>（新增文件）、<strong>删</strong>（删除文件）、<strong>改</strong>（修改文件）。</p><p>点击文件行即可展开内联 Diff，以行级红绿高亮呈现新旧内容对比。点击<strong>展开全部内容</strong>可查看未截断的完整 Diff。</p><hr><h2 id="提交代码" tabindex="-1">提交代码 <a class="header-anchor" href="#提交代码" aria-label="Permalink to &quot;提交代码&quot;">​</a></h2><h3 id="打开提交对话框" tabindex="-1">打开提交对话框 <a class="header-anchor" href="#打开提交对话框" aria-label="Permalink to &quot;打开提交对话框&quot;">​</a></h3><p>点击顶部标题栏的<strong>提交</strong>按钮（或在<strong>更多 Git 操作</strong>菜单中选择<strong>提交</strong>），打开「提交更改」对话框。</p><p>对话框显示：</p><ul><li><strong>当前分支</strong>：本次提交将写入哪条分支</li><li><strong>更改</strong>：将被提交的文件数</li><li><strong>提交消息</strong>输入框：可手动填写，或留空由 AI 自动生成</li></ul><h3 id="ai-生成提交消息-v0-5-0-起" tabindex="-1">AI 生成提交消息（v0.5.0 起） <a class="header-anchor" href="#ai-生成提交消息-v0-5-0-起" aria-label="Permalink to &quot;AI 生成提交消息（v0.5.0 起）&quot;">​</a></h3><p>在「提交更改」对话框中，点击提交消息输入框右侧的 <strong>✨</strong> 按钮：</p><ol><li>按钮进入加载状态，输入框暂时禁用。</li><li>AI 根据当前工作区的实际改动，自动生成一条规范的提交消息并填入输入框。</li><li>你可以在此基础上继续编辑，满意后点击<strong>提交</strong>。</li></ol><blockquote><p>提示文字：「留空以自动生成提交消息」</p></blockquote><p>生成走一次与当前任务隔离的一次性会话，不会出现在任务列表中。生成失败时会显示具体的错误信息，便于排查。</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>生成提交消息会消耗额外 token。你可以直接留空提交消息字段，提交时也会自动生成。</p></div><h3 id="推送与更多-git-操作" tabindex="-1">推送与更多 Git 操作 <a class="header-anchor" href="#推送与更多-git-操作" aria-label="Permalink to &quot;推送与更多 Git 操作&quot;">​</a></h3><p>点击<strong>提交</strong>按钮旁的<strong>更多 Git 操作</strong>按钮，展开操作菜单，包含：</p><ul><li><strong>提交</strong>：等同于点击提交按钮</li><li><strong>推送</strong>：将当前分支推送到 <code>origin</code>，推送期间界面显示「推送中…」提示，完成后显示「已推送到 origin/&lt;分支名&gt;」</li><li><strong>创建分支</strong>：基于当前 HEAD 新建并切换分支（worktree 环境下不可用）</li></ul><p><strong>未推送计数</strong>：当本地有尚未推送的提交时，<strong>推送</strong>菜单项右侧会显示 <code>↑N</code> 徽标——N 表示本地领先远程的提交数（即已提交但还没推送到远程仓库的提交数量）。推送成功后徽标消失。</p><p><img src="'+t+'" alt="截图：顶部标题栏「更多 Git 操作」菜单展开，显示提交、推送、创建分支三个选项"></p><hr><h2 id="回滚变更" tabindex="-1">回滚变更 <a class="header-anchor" href="#回滚变更" aria-label="Permalink to &quot;回滚变更&quot;">​</a></h2><p>在对话中触发代码回滚操作时，界面会显示<strong>本次回滚变更</strong>预览面板，展示将被回滚的文件列表与 Diff。</p><div class="danger custom-block"><p class="custom-block-title">回滚操作不可恢复</p><p>确认回滚后，当前工作区的文件将被还原到指定快照状态，回滚后的更改<strong>无法通过 LoongCode 撤销</strong>。请在确认 Diff 预览后再操作。</p></div><p>回滚仅影响文件内容，不会影响任务的对话历史记录。</p><hr><h2 id="下一步" tabindex="-1">下一步 <a class="header-anchor" href="#下一步" aria-label="Permalink to &quot;下一步&quot;">​</a></h2><p>完成 Git 工作流的学习后，接下来了解如何使用斜杠命令快速调用内置功能——前往<a href="/docs/features/slash-commands.html">斜杠命令</a>继续学习。</p>',60)])])}const u=o(s,[["render",n]]);export{k as __pageData,u as default};
